allOf:
  - $ref: '#/components/schemas/CreateBlobKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        properties:
          value:
            description: arbitrary text
            type: object
            format: json.RawMessage
        required:
        - value
