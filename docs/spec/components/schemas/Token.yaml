allOf:
  - $ref: '#/components/schemas/TokenKey'
  - type: object
    required:
      - attributes
    properties:
      attributes:
        type: object
        properties:
          token:
            description: bearer token
            type: string
        required:
          - token
