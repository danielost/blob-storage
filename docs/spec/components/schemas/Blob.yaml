allOf:
  - $ref: '#/components/schemas/BlobKey'
  - type: object
    required:
      - attributes
    properties:
      # relationships:
      #     type: object
      #     required:
      #       - user
      #     properties:
      #       user:
      #         type: object
      #         properties:
      #           data:
      #             type: object
      #             properties:
      #               $ref: '#/components/schemas/UserKey'
      attributes:
        type: object
        properties:
          value:
            description: arbitrary text
            type: string
            example: foobar
          created_at:
            type: string
            format: time.Time
        required:
          - value
          - created_at
